[pytest]
pythonpath = src
testpaths = tests
python_files = test_*.py *_test.py
asyncio_mode = auto
addopts =
    -v
    -ra
    --cov=src
    --cov-branch
    --cov-fail-under=90
    --cov-report=term-missing:skip-covered
